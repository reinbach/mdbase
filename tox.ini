[pytest]
norecursedirs = .tox
addopts =
        -p no:cacheprovider
        -p no:cache
#        -p no:runfailed
#        -p no:xdist
#        -p no:pep8
        --tb=short
        --capture=no
        --cov-report=html
        --cov-config=tests/.coveragerc
        --cov=mdbase

python_files=test_*.py
